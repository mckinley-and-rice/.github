# 기여 안내

이 조직의 모든 저장소에 적용되는 기본 기여 규약입니다. 자기 `CONTRIBUTING.md` 를 가진 저장소는
그것을 쓰며, 둘이 어긋나면 저장소가 이깁니다. 자기 빌드는 자기가 압니다.

English: [CONTRIBUTING.md](./CONTRIBUTING.md)

## 시작하기 전에

빌드 방법은 저장소의 `README.md`, 그 트리에만 있는 규칙은 `AGENTS.md` 에서 읽습니다.
저장소가 **포크**라면 `UPSTREAM.md` 또는 `docs/UPSTREAM.md` 를 먼저 읽으십시오. 포크에서 가장
파급이 큰 작업은 상류를 흡수하는 일이고, 그것을 지키는 규칙은 선택 사항이 아닙니다.

## 브랜치

Gitflow입니다. `develop` 이 통합하고, `main` 이 릴리스된 상태입니다. 릴리스와 핫픽스를 포함한
전체 모델은 [docs/GITFLOW.ko.md](./docs/GITFLOW.ko.md) 에 있습니다.

작업 브랜치는 `<type>/<short-slug>` 입니다.

```
fix/composer-drop-zone
feat/reasoning-effort
chore/bump-electron
docs/upstream-sync
```

허용되는 타입 목록은 **저장소마다 다르며**, 그 저장소의 `.github/workflows/gitflow.yml` 안
`ALLOWED_TYPES` 에 있습니다. 브랜치 이름을 정하기 전에 그것을 읽으십시오. 어떤 저장소는 `feat`,
어떤 저장소는 `feature` 를 쓰고, 검사는 추측한 이름을 실패시킵니다.

브랜치 이름은 변경이 무엇인지를 말하며, 누가 또는 무엇이 만들었는지는 절대 말하지 않습니다.
`kiro/`, `claude/`, `bot/` 은 타입이 아닙니다. 이 검사가 존재하는 이유는, 검사가 생기기 전에
정확히 그 접두어를 가진 브랜치들이 머지됐기 때문입니다.

## 커밋

Conventional 접두어, 명령형, 소문자 제목:

```
fix(composer): show the drop zone during dragover, not only on drop
```

값은 본문에 있습니다. 무엇이 잘못됐고 왜 이 수정이 옳은 모양인지를 쓰십시오. diff가 이미 보여주는
것을 다시 쓰지 마십시오. 증상이 나타난 곳이 아닌 다른 곳에서 원인을 찾았다면 그것을 쓰십시오.
다음 사람에게 필요한 문장이 바로 그것입니다.

커밋과 풀 리퀘스트 본문은 영어로 씁니다. 이력이 한 언어로 읽히게 하기 위해서입니다. 이슈와
논의는 어느 언어든 괜찮습니다.

em dash는 쓰지 않습니다. 커밋에서도, 다른 어디에서도 그렇습니다. 콜론, 쉼표, 괄호, 또는 문장을
나누는 방법을 쓰십시오.

## 풀 리퀘스트를 열기 전에

저장소 자신의 게이트를 돌립니다. `README.md` 나 `CONTRIBUTING.md` 에 적혀 있습니다. 보통은
이런 모양입니다.

```bash
<install>          # pnpm install / bun install / cargo fetch
<test>
<typecheck>
<lint>
```

그 다음 베이스 브랜치에 리베이스합니다. 브랜치는 한 가지에 집중시킵니다. 리뷰어가 이 브랜치가
무엇을 하는지 한 문장으로 말할 수 있어야 합니다.

**기억하지 말고 재측정하십시오.** 커밋 메시지, 풀 리퀘스트 본문, 체인지로그, 릴리스 노트에 적는
개수, 크기, 버전, 시간은 같은 자리에서 빌드된 산출물로 다시 재고, 어느 표면에서 나온 숫자인지
밝힙니다. 앞선 요약에서 그대로 옮겨 온 숫자는 대개 다른 것을 세고 있습니다.

## 리뷰와 머지

풀 리퀘스트는 검사가 초록불이고 메인테이너가 머지해야 합니다. 여러 저장소에서 메인테이너 한 명의
셀프 머지를 허용하는데, 그것은 인력 사정이지 검사를 건너뛰라는 초대가 아닙니다.

머지 방식은 취향이 아닙니다. 다음 머지가 무엇을 할 수 있는지를 바꿉니다.

| 상황 | 방식 | 이유 |
|---|---|---|
| 작업 브랜치 | **squash** | 하나의 관심사, `develop` 에 하나의 커밋. |
| 상류 싱크 | **merge 커밋** | squash는 merge base를 파괴하고, 다음 싱크가 이미 가져온 작업을 다시 재생합니다. |
| `develop` 을 `main` 으로 승격 | **merge 커밋** | 같은 이유입니다. 그 관계가 핵심입니다. |

뒤의 두 경우는 rebase 머지를 절대 쓰지 않습니다.

빨간 체크는 넘기는 것이 아니라 진단하는 것입니다. 먼저 풀 리퀘스트의 변경 파일을 보십시오.
문서만 바꾼 브랜치가 Windows 유닛 테스트를 실패시킬 수는 없고, 그렇게 보인다면 그 실패는 베이스
브랜치의 것이며 별도 이슈에 속합니다. 두 브랜치가 같은 커밋을 가리키면, 옛 브랜치를 지운 뒤에도
그 브랜치의 실패한 실행이 새 풀 리퀘스트에 붙어 있습니다. head 커밋을 바꿔 지웁니다.

## 시크릿

`.env`, 자격증명 파일, 토큰을 커밋하지 않습니다. 환경변수 값을 로그, 커밋 메시지, 채팅 메시지에
쓰지 않습니다. 새 저장소의 첫 커밋 전에 `.gitignore` 를 확인하고, `node_modules` 라는 이름의
심볼릭 링크가 스테이징되지 않았는지 확인하십시오. 뒤에 슬래시가 붙은 `node_modules/` 는 디렉터리만
매칭하므로 같은 이름의 심볼릭 링크는 그대로 통과합니다.

자격증명이 커밋에 들어갔다고 판단되면 즉시, 그리고 [SECURITY.md](./SECURITY.md) 에 따라
비공개로 알리십시오. 교체는 쌉니다. 조용히 남은 자격증명은 그렇지 않습니다.

## 사용자에게 보이는 문자열

사용자에게 보이는 모든 문자열은 저장소의 번역 계층을 지나고, 영어와 한국어 표에 함께 들어갑니다.
영어가 정본입니다.

완결성 검사를 통과시키려고 기계번역을 밀어넣지 마십시오. 저장소의 번역 baseline에 기록된
번역되지 않은 영어 값이 정직한 상태입니다. 지어낸 한국어 문자열은 누군가 검토한 것처럼 보입니다.

저작권 표기는 번역하지 않습니다. 한국어 문서는 산문은 한국어로 쓰고 표기 줄은 원문 그대로
인용합니다. 번역된 표기 줄은 두 번째의, 다른 권리자로 읽힙니다.

## 디자인 변경

사용자가 보는 것은 모두 [docs/DESIGN.ko.md](./docs/DESIGN.ko.md) 를 따릅니다. 두 규칙이 나머지
전부보다 중요합니다. 토큰 값은 기억으로 다시 타이핑하지 말고 정본 스타일시트에서 가져오고,
결과는 diff가 아니라 빌드된 화면을 보고 검증합니다. 초록불 빌드는 보이지 않는 버튼, 읽을 수 없는
라벨, 아무 동작도 하지 않는 컨트롤을 여러 번 출하했습니다.

## 기여물의 라이선스

기여하면 당신의 작업은 그 저장소 `LICENSE` 의 조건으로 배포되는 데 동의하는 것입니다. 자기 기여의
저작권은 당신이 그대로 갖습니다. 저장소가 포크인 경우 상류 표기는 남고 우리 표기는 그 아래에
쌓입니다. 절대 그 자리를 대신하지 않습니다.
